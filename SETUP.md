# 최초 1회 설정 가이드

이 프로그램이 내 Gmail에서 쿠팡이츠 결제 메일을 읽어오려면, 구글로부터
"이 프로그램이 내 메일을 읽어도 좋다"는 허락을 받아야 합니다. 그 허락을
받기 위한 최초 1회 설정 과정입니다. 한 번만 해두면 그 다음부터는
`run.bat` 더블클릭만 하면 됩니다.

전체 과정은 약 10분 정도 걸립니다.

---

## 1단계. 파이썬 필수 프로그램 설치

1. 이 폴더(`260908_get_order_list`) 안의 **`install.bat`** 파일을 더블클릭합니다.
2. 검은 창이 뜨면서 필요한 프로그램을 자동으로 설치합니다.
3. "설치가 완료되었습니다" 라는 문구가 나오면 아무 키나 눌러 창을 닫습니다.

> 만약 "파이썬이 설치되어 있지 않습니다" 라는 메시지가 뜨면,
> [python.org](https://www.python.org/downloads/) 에서 파이썬을 먼저 설치한 뒤
> `install.bat`을 다시 실행해주세요. 설치할 때 하단의
> **"Add python.exe to PATH"** 체크박스를 꼭 체크해주세요.

---

## 2단계. 구글 클라우드 콘솔에서 앱 등록하기

이 프로그램을 구글에 등록하는 과정입니다. **무료**이며, 등록해도 다른 사람이
내 메일에 접근할 수 있게 되는 것이 **아닙니다** (오직 내가 만든 이 프로그램만
사용 가능).

### 2-1. 프로젝트 만들기

1. 브라우저로 [https://console.cloud.google.com/](https://console.cloud.google.com/) 접속 후, 쿠팡이츠 결제 메일이 오는 구글 계정으로 로그인합니다.
2. 화면 상단의 프로젝트 선택 메뉴 → **"새 프로젝트"** 클릭
3. 프로젝트 이름에 `coupang-eats-csv` 처럼 원하는 이름을 입력하고 **"만들기"** 클릭
4. 화면 상단에서 방금 만든 프로젝트가 선택되어 있는지 확인합니다.

### 2-2. Gmail API 사용 설정

1. 왼쪽 메뉴(≡) → **"API 및 서비스"** → **"라이브러리"** 클릭
2. 검색창에 `Gmail API` 입력 후 검색 결과 클릭
3. **"사용 설정"(Enable)** 버튼 클릭

### 2-3. OAuth 동의 화면 설정

1. 왼쪽 메뉴 → **"API 및 서비스"** → **"OAuth 동의 화면"** 클릭
2. User Type을 **"외부(External)"** 로 선택 후 **"만들기"**
3. 앱 이름: `쿠팡이츠 결제내역 정리` (아무 이름이나 가능), 사용자 지원 이메일과
   개발자 연락처 이메일에 본인 이메일 주소 입력 후 **"저장 후 계속"**
4. "범위(Scopes)" 화면은 그냥 **"저장 후 계속"** (건너뛰어도 됨)
5. "테스트 사용자" 화면에서 **"+ ADD USERS"** 클릭 후 본인 Gmail 주소를
   입력하고 추가 → **"저장 후 계속"**
   - ⚠️ 이 단계가 가장 중요합니다. 본인 이메일을 테스트 사용자로 등록해야
     로그인할 때 "확인되지 않은 앱" 오류 없이 사용할 수 있습니다.

### 2-4. 인증 파일(credentials.json) 만들기

1. 왼쪽 메뉴 → **"API 및 서비스"** → **"사용자 인증 정보"** 클릭
2. 상단 **"+ 사용자 인증 정보 만들기"** → **"OAuth 클라이언트 ID"** 클릭
3. 애플리케이션 유형: **"데스크톱 앱"** 선택
4. 이름은 아무거나 입력(예: `coupang-eats-desktop`) 후 **"만들기"**
5. 생성 완료 팝업에서 **"JSON 다운로드"** 클릭 → 파일이 다운로드됩니다.
6. 다운로드된 파일의 이름을 **`credentials.json`** 으로 바꾸고,
   이 프로젝트 폴더(`260908_get_order_list`) 안으로 옮겨 넣습니다.
   - 최종적으로 `coupang_eats_csv.py` 파일과 같은 폴더 안에
     `credentials.json` 파일이 있어야 합니다.

---

## 3단계. 첫 실행 (로그인)

1. 프로젝트 폴더의 **`run.bat`** 을 더블클릭합니다.
2. "조회할 연도"와 "월"을 입력합니다.
3. 자동으로 브라우저 창이 열리며 구글 로그인 화면이 나옵니다. 쿠팡이츠
   결제 메일이 오는 계정으로 로그인합니다.
4. "Google에서 확인하지 않은 앱입니다" 라는 경고가 뜨면
   → **"고급"** 클릭 → **"(안전하지 않음) coupang-eats-csv(으)로 이동"** 클릭
   (내가 직접 등록한 내 프로그램이라 안전합니다)
5. 권한 요청 화면에서 **"계속"** 클릭해 Gmail 읽기 권한을 허용합니다.
6. 로그인이 끝나면 자동으로 결제 내역을 검색해 CSV 파일을 만듭니다.

이후부터는 로그인 정보가 `token.json` 파일에 저장되어, 다시 `run.bat`을
실행할 때는 로그인 화면 없이 바로 진행됩니다.

---

## 문제가 생겼을 때

- **"credentials.json 파일을 찾을 수 없습니다"** → 2-4단계에서 받은 파일을
  올바른 이름/위치에 넣었는지 확인해주세요.
- **로그인이 계속 실패한다** → `token.json` 파일을 삭제한 뒤 `run.bat`을
  다시 실행해보세요.
- **"파이썬이 설치되어 있지 않습니다"** → 1단계 안내대로 파이썬을 설치해주세요.
